node_modules
npm-debug.log*
.DS_Store
.env
*.local

# SQLite data (default DB_DRIVER) — the DB file + its WAL/SHM sidecars
.volt/*.db
.volt/*.db-*

# Web-admin font override (runtime; the 5050 config writes .env FONT_* instead)
.volt/fonts.json
