{
  "name": "volt-image-host",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Host-side image service for Volt sites — optimize (webp/avif) + store + serve images from ONE shared DigitalOcean Spaces bucket, namespaced per site, with per-site quotas. Standalone: does not touch core Volt.",
  "bin": { "volt-image-host": "server.js" },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "express": "^4.19.2",
    "sharp": "^0.33.5"
  },
  "engines": { "node": ">=18" }
}
