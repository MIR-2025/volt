{
  "name": "volt-ai-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "The voltjs.com hosted AI gateway: holds the capped Anthropic Workspace key, issues/verifies per-app tokens, enforces per-app + global daily token caps, meters usage, kill switch. The key lives only here.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "pm2": "npx --yes pm2 start ecosystem.config.cjs",
    "pm2:restart": "npx --yes pm2 restart ecosystem.config.cjs",
    "pm2:logs": "npx --yes pm2 logs"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "license": "MIT"
}
